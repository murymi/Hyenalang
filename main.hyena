fn main() void {
    if(Args::arg_count() != 2) {
        fmt::println("Usage: %s <filename>\n", .{Args::get_argument_at(0)});
        exit(1);
    }

    fmt::println("%d", .{Args::arg_count()});
    //var slice = Args::get_argument_at(1);
    fmt::println("%s", .{Args::get_argument_at(0)});
}

import "args.hyena"
import "fmt.hyena"
import "file.hyena"