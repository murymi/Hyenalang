fn write(data:&u8) void {
    asm {
        "mov rsi, [rdi+8]"
        "mov rdx, [rdi +0]"
        "mov rdi, 1"
        "mov rax, 1"
        "syscall"
    }
}

struct foo {
    x:u8,
    y:u8
}

fn main() void {

    if(!20) {
        write("hello\n");
    }
}