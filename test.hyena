fn write(data:&u8) void {
    asm {
        "mov rsi, [rdi+8]"
        "mov rdx, [rdi +0]"
        "mov rdi, 1"
        "mov rax, 1"
        "syscall"
    }
}

struct Baz {
    t:u64
}

struct Bar {
    k:Baz
}

struct Foo {
    x:u8,
    y:Bar
}

fn main() void {
    var a = Foo { 
        .x = 8,
        .y = Bar{ 
            .k = Baz{
                 .t = 70 
            }
        }
    };
    
    return a.x + a.y.k.t;
}