fn write(data:&u8) void {
    asm {
        "mov rsi, [rdi+8]"
        "mov rdx, [rdi +0]"
        "mov rdi, 1"
        "mov rax, 1"
        "syscall"
    }
}

struct Bar {
    x:u8,
    y:u8
}

fn makeArray() Bar {
    return Bar { .x = 10, .y = 20 };
}

fn main() void {
    return makeArray().x;
}
