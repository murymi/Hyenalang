fn write(data:&u8) void {
    asm {
        "mov rsi, [rdi+8]"
        "mov rdx, [rdi +0]"
        "mov rdi, 1"
        "mov rax, 1"
        "syscall"
    }
}

struct Baz {
    t:u64
}

struct Bar {
    k:Baz
}

struct Foo {
    x:u8,
    y:u8
}

fn main() void {
    var a = []Foo{Foo { .x = 1, .y = 4},Foo { .x = 1, .y = 4}};
    return a[0].x + a[0].y + a[1].x + a[1].y;
}